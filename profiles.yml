cryptopulse:
  target: dev
  outputs:
    dev:
      type: duckdb
      # Path is relative to where you run dbt from (repo root).
      # GitHub Actions will use the same DuckDB file committed to the repo.
      path: cryptopulse.duckdb
